#include "Arduino.h"
const byte buzzerPin = 2;


void scrollSound(){
    // tone(buzzerPin, 400);
    // delay(100);
    // noTone(buzzerPin);
}

void eatSound(){
    // tone(buzzerPin, 1000);
    // delay(100);
    // noTone(buzzerPin);
}